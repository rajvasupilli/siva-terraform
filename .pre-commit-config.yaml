repos:
- repo: local
  hooks:
    - id: inline-pylint-with-bash
      name: inline-pylint-with-bash
      entry: bash -c 'touch siva.txt && exit 1'
      language: system
      types: [python]
      pass_filenames: false
